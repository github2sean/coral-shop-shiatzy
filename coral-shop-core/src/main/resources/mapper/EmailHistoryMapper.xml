<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dookay.coral.shop.message.mapper.EmailHistoryMapper" >
  
    <!-- 公共查询语句 -->
	<sql id="selectSql">
		select 
		    <trim suffixOverrides="," >
		    	a.`id` as 'id',
		    	a.`create_time` as 'createTime',
		    	a.`email` as 'email',
		    	a.`title` as 'title',
		    	a.`body` as 'body',
		    </trim>
	   	from 
	    	`t_message_email_history` as a
	</sql>
  
</mapper>