<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dookay.coral.shop.goods.mapper.PrototypeSpecificationMapper" >
  
    <!-- 公共查询语句 -->
	<sql id="selectSql">
		select 
		    <trim suffixOverrides="," >
		    	a.`id` as 'id',
		    	a.`name` as 'name',
		    	a.`display_order` as 'displayOrder',
		    	a.`prototype_id` as 'prototypeId',
		    	a.`is_color` as 'isColor',
		    </trim>
	   	from 
	    	`t_goods_prototype_specification` as a
	</sql>
  
</mapper>